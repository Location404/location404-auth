# Database Configuration
DATABASE_CONNECTION_STRING=Host=localhost;Port=5432;Database=location404_auth;Username=postgres;Password=your_password_here

# Cache Configuration (Redis/Dragonfly - optional)
CACHE_CONNECTION_STRING=

# JWT Configuration
JWT_ISSUER=location404
JWT_AUDIENCE=location404
JWT_SIGNING_KEY=your_secret_signing_key_change_in_production
JWT_ACCESS_TOKEN_MINUTES=60
JWT_REFRESH_TOKEN_MINUTES=1440

# CORS Configuration (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:5173
